dbt_localbike:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account-json
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "localbike_dev"
      threads: 4
      keyfile_json:
        type: "{{ env_var('GCP_TYPE') }}"
        project_id: "{{ env_var('GCP_PROJECT_ID') }}"
        private_key_id: "{{ env_var('GCP_PRIVATE_KEY_ID') }}"
        private_key: "{{ env_var('GCP_PRIVATE_KEY') }}"
        client_email: "{{ env_var('GCP_CLIENT_EMAIL') }}"
        client_id: "{{ env_var('GCP_CLIENT_ID') }}"
        auth_uri: "{{ env_var('GCP_AUTH_URI') }}"
        token_uri: "{{ env_var('GCP_TOKEN_URI') }}"
        auth_provider_x509_cert_url: "{{ env_var('GCP_AUTH_PROVIDER_X509_CERT_URL') }}"
        client_x509_cert_url: "{{ env_var('GCP_CLIENT_X509_CERT_URL') }}"
    prod:
      type: bigquery
      method: service-account-json
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "localbike_prod"
      threads: 4
      keyfile_json:
        type: "{{ env_var('GCP_TYPE') }}"
        project_id: "{{ env_var('GCP_PROJECT_ID') }}"
        private_key_id: "{{ env_var('GCP_PRIVATE_KEY_ID') }}"
        private_key: "{{ env_var('GCP_PRIVATE_KEY') }}"
        client_email: "{{ env_var('GCP_CLIENT_EMAIL') }}"
        client_id: "{{ env_var('GCP_CLIENT_ID') }}"
        auth_uri: "{{ env_var('GCP_AUTH_URI') }}"
        token_uri: "{{ env_var('GCP_TOKEN_URI') }}"
        auth_provider_x509_cert_url: "{{ env_var('GCP_AUTH_PROVIDER_X509_CERT_URL') }}"
        client_x509_cert_url: "{{ env_var('GCP_CLIENT_X509_CERT_URL') }}"